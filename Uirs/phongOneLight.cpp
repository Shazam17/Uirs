/*

		//lightPos.x = radius * cos(time * speed);
		//lightPos.y = radius * sin(time * speed);
		//lightPos.z = radius * sin(time * speed)*cos(time * speed);
		//glm::mat4 modelC = glm::mat4(1.0f);
		//modelC = glm::translate(modelC, lightPos);
		//modelC = glm::scale(modelC, glm::vec3(0.3f));
		//shadLighter.setUniform4m("u_model", modelC);*/